---
const { title } = Astro.props;
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1);
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Haven Bookstore" />
		<meta name="viewport" content="width=device-width" />
		<link rel="apple-touch-icon" sizes="180x180" href="apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="favicon-16x16.png">
    <link rel="manifest" href="site.webmanifest">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
		<title>{title}</title>
	</head>
	<body>
    <header>
      <nav>
        <a class="mobile-logo" href=""><img src="/icons/logo.svg" alt="Home" class="logo" height="62"></a>
        <button class="nav-button"><img src="/icons/menu.svg" alt="Navigation Menu" class="nav-icon"></button>
        <div class="nav-links">
          <a class={currentPath === "bookstore-site" ? "nav-link active" : "nav-link"} href="/about">About</a>
          <a class={currentPath === "bookstore-site" ? "nav-link active" : "nav-link"} href="/events">Events</a>
          <a class="nav-link-logo" href=""><img src="/icons/logo.svg" alt="Home" class="logo" height="68"></a>
          <a class={currentPath === "bookstore-site" ? "nav-link active" : "nav-link"} href="/gallery">Gallery</a>
          <a class={currentPath === "bookstore-site" ? "nav-link active" : "nav-link"} href="/contact">Contact</a>
        </div>
      </nav>
    </header>
    <div class="content-container">
      <slot />
    </div>
		<footer>
			<div class="footer-container">
        <div class="footer-contact">
          <p>Contact us</p>
          <address>1234 Book Street<br/>
          email@email.com<br/>
          111-222-333<br />
          Open 8AM-7PM
          </address>
        </div>
        <a class="footer-logo" href=""><img src="icons/logo_dark.svg" alt="Haven Bookstore Home" class="logo" height="68"></a>
        <div class="footer-socials">
          <p>Follow us on</p>
          <div class="socials">
            <a class="social link" href="" ><img src="/icons/instagram.svg" alt="Instagram" class="social" height="37"></a>
            <a class="social link" href="" ><img src="/icons/facebook.svg" alt="Facebook" class="social" height="37"></a>
            <a class="social link" href="" ><img src="/icons/twitter.svg" alt="Twitter" class="social" height="37"></a>
          </div>
        </div>
      </div>
		</footer>
	</body>
</html>
<style is:global>
  @font-face {
    font-family: 'Libre Baskerville';
    src: url('fonts/libre-baskerville-v14-latin-regular.woff2') format('woff2');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Merriweather';
    src: url('fonts/merriweather.woff2') format('woff2');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
    size-adjust: 90%;
  }
  :root {
    font-size: 120%;
    --heading-font: 'Libre Baskerville', serif;
    --body-font: 'Merriweather', serif;
    --sand: #e7d8d0;
    --dark-sand: #d6bcae;
    --light-blue: #d0dfe7;
  }
  html {
    font-family: var(--body-font);
  }
  h1, h2, h3, h4, h5, h6 {
    font-family: var(--heading-font);
  }
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  @media (max-width: 1000px) {
    :root {
      font-size: 110%;
    }
  }
</style>
<style>
   body {
    height: 100vh;
    display: flex;
    flex-direction: column;
  }
  header {
    padding: 2rem;
  }
  nav {
    margin: 0 auto;
    max-width: 1000px;
  }
  .mobile-logo {
    display: none;
  }
  .nav-button {
    margin-left: auto;
    display: none;
    background-color: inherit;
    border: none;
  }
  .nav-icon {
    width: 2rem;
  }
  .nav-links {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  nav a {
    color: black;
    font-size: 1.25rem;
    text-decoration: none;
  }
  .logo {
    width: 9rem;
  }
  .nav-links {
    display: flex;
  }
  .content-container {
    flex: 1;
    height: auto;
    background-color: var(--sand);
  }
  footer {
    background-color: black;
    padding: 2rem;
    color: white;
  }
  footer p {
    font-size: 1.1rem;
  }
  .footer-container {
    margin: 0 auto;
    max-width: 1000px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .footer-contact {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .footer-socials {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .socials {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }
  .social {
    width: 2rem;
  }
  @media (max-width: 600px) {
    header {
      position: relative;
      z-index: 5;
    }
    nav {
      margin: 0;
      display: flex;
    }
    .mobile-logo {
      display: inline;
    }
    .nav-button {
      display: inline-block;
    }
    .nav-link-logo {
      display: none;
    }
    .nav-links {
      z-index: 6;
      opacity: 90%;
      background-color: white;
      display: none;
      margin-top: 5.8rem;
      padding: 2rem;
      width: 100%;
      position: absolute;
      left: 0;
      flex-direction: column;
      gap: 1rem;
      align-items: center;
    }
    .footer-container {
      flex-direction: column;
      gap: 1rem;
    }
  }
  .nav-links-toggle {
    display: flex;
  }
</style>
<script>
  document.querySelector('.nav-button').addEventListener('click', (e) => {
    e.currentTarget.nextElementSibling.classList.toggle('nav-links-toggle');
  })
</script>
